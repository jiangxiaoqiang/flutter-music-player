name: music_player
description: A new Flutter plugin.
version: 0.0.1
author:
homepage:

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  logging: ^1.0.1
  system_clock: ^1.2.0-nullsafety.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    androidPackage: tech.soit.quiet
    pluginClass: MusicPlayerUiPlugin
